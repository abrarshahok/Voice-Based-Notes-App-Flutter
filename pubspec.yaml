name: voice_based_notes_app
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  avatar_glow: ^2.0.2
  flutter:
    sdk: flutter
  flutter_feather_icons: ^2.0.0+1
  flutter_launcher_icons: ^0.13.1
  google_fonts: ^5.1.0
  http: ^1.1.0
  intl: ^0.18.1
  provider: ^6.0.5
  shared_preferences: ^2.2.0
  speech_to_text: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

flutter_icons:
  android: true
  image_path: "assets/icon/notes.png"
